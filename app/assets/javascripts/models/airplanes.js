var app = app || {};

app.Airplane = Backbone.Model.extend({
	urlRoot: '/airplanes',
	// defaults: {
	// 	flight_number: '', origin: '', destination: '', airplane_id: '', date: '', rows: ''
	// }
});
